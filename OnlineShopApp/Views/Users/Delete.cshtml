@model OnlineShopApp.Models.UserViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Client - Delete</h2>

<h3>Are you sure you want to delete this?</h3>
@Html.ValidationSummary(true, "", new { @class = "text-danger" })
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.UserName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.SelectedRoleName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SelectedRoleName)
        </dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <div class="clearfix">
                <input type="submit" value="Delete" class="btn btn-success my-button float-right" />
            </div>
            <a class="btn btn-success my-button" href="@Url.Action("Index", "Users")"><i class="fas fa-arrow-alt-circle-left"></i> Back to List</a>
            <div class="margin-top-10px"></div>
        </div>
    }
</div>
